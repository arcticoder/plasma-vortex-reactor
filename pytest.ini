[pytest]
markers =
    production: marks tests that validate production/high-load behavior
    slow: marks tests as slow (deselect with '-m "not slow"')
    hardware: marks tests that exercise hardware runner/timeout logic
addopts = -m "not slow"
